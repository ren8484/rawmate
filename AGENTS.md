# RAWMate PC 开发规则

本文件是本仓库内所有 AI 编程助手和协作者必须优先阅读并长期遵守的规则。它只约束 Windows PC 版 RAWMate；其他平台不属于本仓库范围。

## 1. 项目源与产物

- 唯一正式源码：`RAWMate.cs`
- 正式图标：`RAWMate.ico`
- 界面徽标：`RAWMateHeader.png`
- 固定测试产物：`RAWMate-Test.exe`
- 正式产物：`RAWMate.exe`
- 当前标准工作目录：`C:\rawmate`
- 测试构建入口：`BUILD_RAWMate-Test.cmd`
- 正式构建入口：`BUILD_RAWMate.cmd`
- 原始本地工作目录可能保留 `RAW_JPG_整理助手.py`、`启动_RAW_JPG_整理助手.cmd` 和旧 `使用说明.md`。它们是早期 Python 原型，只作历史参考，已被 `.gitignore` 排除，不是后续开发入口。

不要从 EXE 反向修改项目，不要另建含有同一套业务逻辑的平行源码。若以后拆分 `RAWMate.cs`，必须在同一个变更中同步更新 `PROJECT_CONTEXT.md` 和 `docs/ARCHITECTURE.md`。

## 2. 强制工作流

1. 先阅读 `PROJECT_CONTEXT.md`、`README.md`、`CHANGELOG.md` 及与任务有关的 `docs/` 文件。
2. 先检查当前源代码和工作区状态，保留与任务无关的已有改动。
3. 只在 `RAWMate.cs` 和任务直接涉及的文档/脚本中修改。
4. 先构建固定名称 `RAWMate-Test.exe`，不得直接覆盖正式版。
5. 按 `docs/TESTING.md` 运行静态、功能和界面核验；涉及竖构图、缩放、滚动条或主题时必须做对应视觉检查。
6. 将测试结果交给用户确认。用户明确同意正式发布后，才可运行 `BUILD_RAWMate.cmd` 覆盖 `RAWMate.exe`。
7. 正式发布后同步 `CHANGELOG.md`；若产品行为或长期决策改变，还要同步 `PROJECT_CONTEXT.md` 和相关 `docs/`。
8. 若创建桌面快捷方式，名称只能是 `RAWMate`，不添加“照片工具”等后缀，目标必须是正式 `RAWMate.exe`。
9. 日常开发只在 `C:\rawmate` 进行；旧目录是历史快照，不再提交或构建。
10. 左侧栏左下角必须常驻显示当前产品版本；文字从 `AssemblyInformationalVersion` 自动生成，三段版本 `1.1.0` 显示为 `V1.10`。发布新版本时必须先同步程序集版本元数据，禁止另行硬编码界面版本号。

## 3. 文件安全边界

以下规则不可因 UI 重构或“简化逻辑”而破坏：

- 分类只处理用户所选根目录顶层的 `.jpg`、`.jpeg` 和 `.arw`；不递归扫描其他子目录。
- JPG/JPEG 移到根目录下 `jpg`，ARW 移到根目录下 `arw`。
- 目标存在同名文件时必须跳过，绝不覆盖。
- JPG 与 ARW 按不区分大小写的文件名主体配对，如 `DSC0001.JPG` 对应 `DSC0001.ARW`。
- 所有清理动作必须进入 Windows 回收站；程序不得提供或调用永久删除。
- 删除/淘汰前必须清楚告知将处理的 JPG 数、配对 ARW 数，并要求用户确认。
- “检查配对”可报告两种缺失；只有“无同名 JPG 的 ARW”可以在用户确认后移入回收站，缺少 ARW 的 JPG不得自动处理。
- 批量操作必须逐个记录失败，并在结束时明确提示，不得把部分成功报告成全部成功。

## 4. 交互与界面约束

- 整体保持 Lightroom 风格的低干扰照片挑选界面：固定左侧功能区，右侧为图库或单张主视图。
- 左侧功能区不得出现无意义的滚动条；右侧需要滚动时使用窄、深色、圆角的迷你滚动条。
- 避免多层卡片、粗白边、大面积 banner、无意义占位文案和三层按钮色阶。
- 同类操作按钮保持同一蓝色视觉体系；禁用态用透明度表达。
- 界面固定使用暗黑模式，不提供主题切换；路径和界面状态需在关闭后恢复。
- 网格单击应单选；仅 `Ctrl` 或 `Shift` 扩展多选。双击进入单张视图。
- 右键菜单在网格和单张视图都应可用，菜单不得出现系统默认白色装饰条。
- 竖构图与横构图必须按照 EXIF Orientation 正确显示；网格、单张、导航器和胶片条应使用一致方向。
- 单张视图切换照片时保持当前缩放比例；“适应”才重新计算适应比例。
- 导航器的视口框必须同时适配横构图和竖构图，并能拖动定位主视图。
- 变更界面后优先实际启动测试版，在目标 DPI/窗口尺寸下查看屏幕，而不是只凭代码判断。

## 5. 快捷键与挑片语义

- `P`：切换保留旗标。
- `X` / `Delete`：切换废片旗标，不立即删除。
- `0`：清除星级；`1`–`5`：设置星级。
- `←` / `→`：切换照片。
- `Space`：切换网格与单张视图。
- `Caps Lock`：切换自动前进；开启后 `P`、`X/Delete`、`1`–`5` 完成标记或评分后前进一张，末张不循环，`0` 不前进。
- “移除所有废片”才批量将废片 JPG 和同名 ARW 移入回收站。

不要把 `Delete` 改成无确认的物理删除。

## 6. 技术与兼容性

- 当前实现是无第三方依赖的单文件 C# WPF 程序，使用 Windows 自带 .NET Framework 64 位编译器。
- 保持 C# 语法与 `%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\csc.exe` 兼容，除非用户明确批准迁移技术栈。
- 不要引入 NuGet、安装器、数据库、遥测或网络访问，除非有明确需求和迁移方案。
- 用户当前相机（Sony ILCE-6700）与显示器工作流均按 sRGB 使用；当前 WIC 载图设置忽略嵌入色彩配置。改变色彩管理前需说明预期差异并用真实照片核验。
- 异常日志只写入 `%LOCALAPPDATA%\RAWMate\error.log`，不得记录照片内容或上传数据。

## 7. 提交与 GitHub 规则

- 一个提交只解决一个清晰问题；提交信息说明用户可见结果。
- 不提交 `RAWMate.exe`、`RAWMate-Test.exe`、本机日志或 `%LOCALAPPDATA%` 状态文件。EXE 通过 GitHub Actions artifact 或 Release 分发。
- PR 必须包含：问题、变更范围、安全影响、测试证据、界面截图（若涉及 UI）。
- 未经用户明确确认，不推送远程、不创建正式 Release、不覆盖正式 EXE。
- 不把其他平台的代码、产物或计划写入本仓库的功能完成清单。
