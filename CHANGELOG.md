# Changelog

本项目从此文件开始记录 Windows PC 版的可追溯变更。早期内容依据现有源码和开发过程重建；后续正式版本应使用语义化版本号和 Git 标签。

## [Unreleased]

当前测试候选为 `1.4.1`，通过 `rawmate/test` 分支和 GitHub Actions 的 `RAWMate-Test` 构建产物提供测试；正式版仍为 `1.4.0`。

### Changed

- 挑片模式完成最后一张未决定照片后，在用户确认“挑片完成”汇总时自动切换为“浏览”，当前照片保持不变；若原来筛选“未决定”，同时恢复“全部”筛选，随后可用前后导航逐张复查整批照片。
- “挑片 / 浏览”改为按当前目录工作阶段初始化：存在未决定照片时进入挑片，已全部决定时进入浏览；不再通过 `advance_target` 把上一批照片的导航状态带到其他目录。

### Fixed

- “挑片完成”只在未决定照片由 1 张变为 0 张时触发；已完成批次中的 P/J/R/X 改判不再重复弹框。
- 浏览中取消决定会保持浏览模式，并提示当前未决定数量及可切换到“挑片”继续处理，不再突然改变用户的复查节奏。

## [1.4.0] - 2026-09-07

### Added

- 挑片终态扩展为 `P` 双格式、`J` 仅 JPG、`R` 仅 RAW、`X` 全部废弃；`J` / `R` 同时支持右键菜单和键盘快捷键，挑片阶段只记录决定。
- 新增“执行清理”：确认后按当前目录的 J/R/X 决定集中移入 Windows 回收站；完成汇总显示双格式、仅 JPG、仅 RAW 的细分数量。
- 新增可跨测试版/正式版复用的持久缩略图缓存，以及带文件时间戳校验的 EXIF 拍摄时间缓存。

### Changed

- “检查配对”升级为只读的“文件对应检查”，主动形成的仅 JPG / 仅 RAW 被视为正常结果；文件状态与保存决定矛盾时才报告异常。
- “选中项移至回收站”改为“选中项标记废片”，避免挑片阶段立即移动文件。
- 决定完成量改为 `P + J + R + X`，左侧“保留”汇总为 `P + J + R`。
- 单张视图的当前状态只显示“双格式 / 仅 JPG / 仅 RAW / 废片 / 未决定”等中文结果，不在状态前重复快捷键；右键菜单精简并固定为“标记为废片 / 保留双格式 / 保留 JPG / 保留 RAW”，不显示快捷键字母或“打开原图”。
- 右键菜单恢复 Windows 菜单式的即时悬停反馈：普通项不固定标色，鼠标移到哪一项，只有该行使用与主按钮相同的 `#436794` 蓝色和白字；移开后立即恢复暗色，并移除系统白色焦点框。

### Fixed

- “移除所有废片”只从当前所选目录的 `jpg` 文件夹生成候选，不再让其他目录持久化的 X 记录参与当前目录配对与清理。
- 所有回收站入口统一按实际结果清理挑片状态：只有成功进入回收站的 JPG 才移除对应 P/X；失败或操作前已不存在的 JPG 保留原状态，并计入失败列表。
- 回收站批处理结果改为保留精确的成功与失败路径，避免部分失败时按候选列表错误清除状态。
- 清理完成后不再清空整目录的缩略图与原图缓存，只淘汰实际成功移除的 JPG；网格标记也不再重建整个窗口。
- 当前照片在清理中成功移除后，按清理前的当前排序进入下一张仍存在的照片并支持末尾回绕，不再回到目录第一张；清理失败时停留仍存在的当前照片。
- J/R/X 清理在确认后再次校验目录、配对、决定和文件时间戳；重复执行保持幂等，失败项保留决定以便重试。

## [1.3.1] - 2026-09-05

### Changed

- 单张视图新增可记忆的“挑片 / 浏览”导航目标：“挑片”让左右键及前后按钮只在未决定照片间跳转，标记后继续寻找下一张未决定；“浏览”按当前排序逐张前后切换，标记后直接进入紧邻的下一张照片。
- 单张视图右上角的当前状态、导航目标、网格视图和单张视图四个控件统一为相同高度与等距排列；完整说明移入悬停提示。

### Fixed

- 单张视图直接更新 P/X 状态时不再重建整套胶片条，避免标记后缩略图重新加载造成数秒停顿。
- 网格缩略图改为分批、限并发解码，并优先使用 JPEG 内嵌预览，降低大目录首次加载和网格/单张切换时的阻塞。
- 单张视图会显示当前“保留 / 废片 / 未决定”状态，状态控件不再压到横构图照片上；“移除所有废片”在单张视图存在废片时也能直接使用。

### Compatibility

- 程序集与正式版本更新为 `1.3.1`（Windows 文件版本 `1.3.1.0`）；继续兼容既有 P/X 旗标和本地设置，照片分类、配对与回收站安全边界不变。

## [1.3.0] - 2026-08-27

### Changed

- `P`、`X/Delete` 的键盘挑片流程改为固定自动前进：在“全部 / 未决定”筛选下，按当前排序从完整图库寻找下一张未决定照片，跳过已有结果的照片，并在到达末尾后从开头继续查找。
- 重复当前旗标快捷键会取消决定并停留当前照片；从保留改为废片或从废片改为保留时，仍继续寻找下一张未决定照片。
- “保留 / 废片”筛选明确作为复查模式，修改旗标后只刷新结果，不自动跳往未决定照片。
- 全部照片决定完成后显示“挑片完成”汇总，列出已决定、保留和废片数量；提示框不执行任何文件清理。
- 程序集与正式版本更新为 `1.3.0`（Windows 文件版本 `1.3.0.0`）。

### Removed

- 移除网格和单张视图顶部的自动前进按钮、`Caps Lock` 快捷键以及持久化的自动前进开关。旧 `auto_advance` 设置会被安全忽略，并在下次保存设置时移除。

### Compatibility

- 保持既有 P/X 旗标文件兼容，分类、配对及所有清理仅进入 Windows 回收站的安全边界不变。

## [1.2.2] - 2026-08-20

### Fixed

- 曝光时间不再直接显示相机写入的未约分 EXIF Rational；小于 1 秒统一换算为最接近的 `1/N 秒`，大于等于 1 秒显示秒数。
- 单张视图顶部的超长文件名限制在中间自适应列内，使用单行字符省略号并保留完整名称悬停提示，不再挤占右侧控制按钮。
- 图库卡片的文件名改为单行居中显示；超长名称使用字符省略号和完整名称悬停提示，不再影响卡片宽度与相邻布局。

### Changed

- 调整左栏文字层级：路径与辅助信息使用常规字重，目录提示更轻；P/X 徽标与结果数字使用半粗体，标签保持常规字重，章节标题和按钮统一保留半粗体。
- 界面徽标改为内嵌资源，正式版以单个 `RAWMate.exe` 分发，不再需要旁置 `RAWMateHeader.png`。
- 程序集与正式版本更新为 `1.2.2`（Windows 文件版本 `1.2.2.0`）。

## [1.2.1] - 2026-08-19

### Changed

- 主窗口改为更极简的平面双栏布局，移除左侧与主工作区的外层圆角卡片、重复边框和多余窗口内边距。
- 图库、单张预览与照片信息区使用背景和细分隔线建立层级，保留导航器、缩放控制和胶片条等必要的局部控件。
- 原生 Windows 标题栏改为与固定暗黑界面连续的深色，保留程序图标、白色标题和系统窗口按钮。
- 左下角产品版本改为完整三段显示；程序集信息版本 `1.2.1` 显示为 `V1.2.1`，不再合并次版本和补丁版本数字。
- 程序集与正式版本更新为 `1.2.1`（Windows 文件版本 `1.2.1.0`）。

### Compatibility

- 保持既有 JPG/ARW 分类、P/X 挑片、回收站安全边界、状态文件格式和无第三方依赖的 WPF 架构不变。

## [1.2.0] - 2026-08-19

### Removed

- 移除 0–5 星级、已评分筛选、星级徽标、键盘评分和独立评分统计，挑片工作流收敛为 P/X 二选一。

### Changed

- “未标记 / 未处理”统一改为“未决定”；挑片进度改为“已决定 / 总数”，已决定数只由保留与废片组成。
- 进度条下方收敛为无卡片背景的 `P 保留`与`X 废片`两个等宽结果区，不再重复显示可由进度直接推算的未决定数量。
- 自动前进只由 `P`、`X/Delete` 切换旗标触发；P/X 继续互斥，再按一次相同按键会取消旗标。
- 程序集与正式版本更新为 `1.2.0`（Windows 文件版本 `1.2.0.0`）。

### Compatibility

- 继续读取 1.1.x `cull-marks.txt` 中的 P/X；旧 `R` 记录和损坏的非 P/X 行会被安全忽略，不阻断其他有效旗标。

## [1.1.2] - 2026-08-19

### Added

- 最近目录下拉菜单新增圆角“清除路径记录”按钮；清除后当前目录保持可用，关闭重开时不再恢复旧路径，重新选择或刷新目录后可重新开始记忆。

### Fixed

- 修复在单张视图中切换照片目录后仍显示旧目录照片的问题；新目录会自动加载排序后的第一张 JPG，无 JPG 时返回空图库，同目录刷新则保留仍然存在的当前照片。

### Changed

- 单张视图点击缩放改为自适应检查倍率：适应比例低于 100% 时切换到 100%，达到或超过 100% 时切换到 200%；再次点击直接返回适应，导航器定位沿用同一规则。

## [1.1.1] - 2026-08-18

### Fixed

- 修复 4K/高窗口下左侧功能区因缩放容器垂直居中而残留已删除品牌区高度的问题。

### Changed

- 左侧栏左下角常驻显示从程序集信息版本自动生成的产品版本号；当前 `1.1.1` 显示为 `V1.11`。
- 单张视图点击主图时改为在 100% 与适应比例之间切换，不再默认放大到 200%。

## [1.1.0] - 2026-08-13

### Added

- 新增可记忆的“自动前进”模式；开启后使用 `P`、`X/Delete` 或 `1`–`5` 完成标记/评分会自动选择当前排序中的下一张照片。
- `Caps Lock` 可随时切换自动前进，网格与单张视图顶部均显示当前开关状态。
- 自动前进到当前序列最后一张时停止，不循环回第一张；`0` 清除星级不触发前进。
- 新增“取消分类”，可将 `jpg` / `arw` 中的照片安全还原到所选根目录；发现重名冲突时在移动前停止。
- 左侧栏新增挑片进度：显示已处理数/总数、条内百分比，以及保留、废片、未处理和已评分数量；星级作为独立维度，不计入处理进度。

### Changed

- 图库的时间排序改为读取 JPG EXIF 拍摄时间，不再使用文件修改时间；缺少拍摄时间的照片排列在末尾。
- 界面固定为暗黑模式并移除主题切换按钮。
- 原“分类”按钮改名为“创建分类”，并与“取消分类”组成双向操作。
- 目录操作改为同一行的“选择目录 / 刷新目录”，分类操作改为同一行的“创建分类 / 取消分类”，减少左栏纵向占用。
- 所有按钮统一为深蓝色 `#436794`，悬停、按下和开关状态不再反转底色，禁用态仅降低透明度。
- 左侧栏移除重复的产品名和副标题，目录路径输入区、下拉按钮和加粗后的进度条使用完整圆角。
- 主窗口启动尺寸受当前显示器工作区约束；低高度或高 DPI 时左侧内容只缩小不放大，兼容 1920×1080、150% 缩放，同时保持 4K 原始布局尺寸。

### Fixed

- 修复低分辨率或高缩放显示器上窗口超出工作区、左侧底部操作显示不完整的问题。
- 修复目录路径文字垂直偏上、100% 进度条右端方角及百分比未居中的视觉问题。

### Compatibility

- 保持单文件、零第三方依赖的 WPF 架构，并继续兼容 Windows 自带 .NET Framework 64 位编译器。
- 保持分类不递归、不覆盖同名文件，以及所有清理仅进入 Windows 回收站的安全边界。

## [1.0.0] - 2026-08-12

### Added

- 为正式 EXE 增加产品名称、说明、公司和版权元数据。
- Windows 文件版本设为 `1.0.0.0`，产品版本设为 `1.0.0`。
- 建立 PC 版独立的长期项目上下文、AI 开发规则、架构、测试和发布文档。
- 加入固定测试构建脚本以及 GitHub Issue、PR 和 Windows CI 配置。
- 明确本仓库只维护 Windows PC 版。

## [PC snapshot 2026-07-31]

### Added

- JPG/JPEG 与 Sony ARW 顶层分类到 `jpg` / `arw`。
- 网格与单张视图、主题切换、最近 3 个目录与会话恢复。
- 缩略图大小、名称/时间排序和旗标/评分筛选。
- P/X 旗标、0–5 星级、键盘导航和多选。
- 单张适应/缩放/平移、导航器、胶片条和相邻照片预载。
- 拍摄日期、相机型号、ISO、焦段、光圈和曝光时间。
- EXIF Orientation 1–8 方向处理。
- 右键打开、旗标、删除 JPG、删除 JPG + 同名 ARW。
- 选中照片和所有废片批量移入 Windows 回收站。
- JPG/ARW 双向缺失配对检查与无主 ARW 清理。
- 统一蓝色操作按钮、浅色/暗色主题和迷你滚动条。

### Safety

- 分类不递归、不覆盖同名文件。
- 删除统一使用 Windows 回收站，不提供永久删除。
- 批量清理前确认，并显示配对数量与失败项。

### Performance

- 网格和胶片条缩略图延迟加载。
- 完整图异步加载、最近 5 张 LRU 缓存和前后照片预载。
- 避免在切换单张视图时重复构建隐藏网格。

## [Prototype 2026-07-29]

- Python/Tk 原型验证基本分类、JPG 缩略图、JPG+ARW 配对清理流程。
- 之后由原生 WPF 单 EXE 完整替代；原型不再维护。
