# RAWMate 使用说明

直接双击 `RAWMate.exe` 即可运行。这是原生 Windows 图形界面程序，不需要 Python，也不会弹出命令行窗口。

当前版本：`1.1.1`（Windows 文件版本 `1.1.1.0`）。

## 工作流程

1. 点击“选择目录”，选择相机照片所在的主目录。
2. 可点击“刷新目录”重新扫描；点击“创建分类”，程序会把该目录顶层的 JPG/JPEG 与 ARW 分别移到 `jpg`、`arw` 子目录，需要恢复时点击“取消分类”。
3. 在图库中浏览 JPG；单击选择，`Ctrl` / `Shift` 多选，双击进入单张视图。
4. 按 `P` 标记保留，按 `X` 或 `Delete` 标记废片，按 `1`–`5` 评分。
5. 左侧“挑片进度”会显示已处理比例及保留、废片、未处理、已评分数量；已评分可以与 P/X 旗标并存。
6. 挑选完成后，点击“移除所有废片”，废片 JPG 与同名 ARW 会一起进入 Windows 回收站。
7. 若曾在资源管理器里只删除 JPG，点击“检查配对”，确认后可把无主 ARW 移入回收站。

## 单张浏览

- 左右方向键切换照片。
- 空格切换网格与单张视图。
- 单击图片在“适应”与 100% 间切换。
- 可使用 +/- 或直接输入缩放比例。
- 导航器可定位放大后的图像区域。
- 底部胶片条可快速切换照片。
- 开启“自动前进”后，`P`、`X/Delete`、`1`–`5` 标记或评分后会前进一张；`Caps Lock` 可切换该模式，`0` 不触发前进。

## 安全边界

- 所有清理动作都使用 Windows 回收站，可以恢复；程序不会永久删除照片。
- 分类只处理所选目录顶层的 `.jpg`、`.jpeg`、`.arw`，不会递归修改其他目录。
- 程序不会覆盖同名目标文件。
- JPG 与 ARW 按不区分大小写的文件名主体配对，例如 `DSC0001.JPG` 对应 `DSC0001.ARW`。

更多说明见 `README.md`。
