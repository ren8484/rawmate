# RAWMate 使用说明

直接双击 `RAWMate.exe` 即可运行。这是原生 Windows 图形界面程序，不需要 Python，也不会弹出命令行窗口。

RAWMate 是 JPG + RAW 双格式拍摄的去留筛选器，不是 Lightroom 的前置工具。它负责用 JPG 完成取舍，并让同名 RAW 跟随保留或淘汰；挑完后的照片可以进入任何后期或分享流程。

当前正式版本：`1.4.1`（Windows 文件版本 `1.4.1.0`）。

## 工作流程

1. 把相机拍摄的 JPG 和 ARW 放在同一个主目录中，然后在 RAWMate 里选择这个目录。路径下拉菜单可以切换最近使用的目录，也可以清除路径记录。
2. 点击“创建分类”。程序会把主目录顶层的 JPG/JPEG 移到 `jpg`，把 ARW 移到 `arw`；需要恢复原来的目录结构时，可以点击“取消分类”。
3. 在图库里用 JPG 挑片。按 `P` 保留 JPG+RAW，按 `J` 仅保留 JPG，按 `R` 仅保留 RAW，按 `X` 或 `Delete` 两种都不要；这些决定互斥，再按一次相同按键可以撤销并停留原图。
4. 全部照片决定完成后，程序会显示 P/J/R/X 汇总；确认后保持当前照片并自动进入“浏览”，可用前后导航逐张复查整批照片。
5. 复查无误后点击“执行清理”，程序才按 J/R/X 决定把不保留的文件移入 Windows 回收站。P 不动，J 回收同名 RAW，R 回收 JPG，X 回收两者。
6. 清理完成后，可以把保留的 `arw` 导入 Lightroom 或其他 RAW 软件，也可以从 `jpg` 复制照片到手机或社交平台。

“文件对应检查”只读报告完整配对、仅 JPG、仅 RAW 和异常；它不会自动移动任何照片。由 J/R 清理主动形成的单格式文件属于正常结果。

图库中单击可选择照片，`Ctrl` / `Shift` 用于多选，双击可进入单张视图。点击“刷新目录”可以重新扫描当前目录。

## 单张浏览

- “挑片”导航下，左右方向键只切换上一张/下一张未决定照片；“浏览”导航下，左右方向键按当前排序逐张切换。
- 空格切换网格与单张视图。
- 单击图片在“适应”与自适应检查倍率间切换：适应比例低于 100% 时使用 100%，达到或超过 100% 时使用 200%。
- 可使用 +/- 或直接输入缩放比例。
- 导航器可定位放大后的图像区域。
- 底部胶片条可快速切换照片。
- “挑片”下，在“全部 / 未决定”筛选中按 `P`、`J`、`R` 或 `X/Delete` 会自动跳到下一张未决定照片；“浏览”下则按当前排序直接进入下一张。两种导航到末尾后都会从开头继续，全部完成时显示汇总提示并自动进入“浏览”。
- “保留 / 废片”筛选用于复查，修改旗标后不会自动跳走。

## 安全边界

- 所有清理动作都使用 Windows 回收站，可以恢复；程序不会永久删除照片。
- 分类只处理所选目录顶层的 `.jpg`、`.jpeg`、`.arw`，不会递归修改其他目录。
- 程序不会覆盖同名目标文件。
- JPG 与 ARW 按不区分大小写的文件名主体配对，例如 `DSC0001.JPG` 对应 `DSC0001.ARW`。
- “执行清理”只处理当前所选目录，确认后仍会复核路径、决定、配对和文件时间戳；失败项会保留原决定并明确报告。

更多说明见 `README.md`。
